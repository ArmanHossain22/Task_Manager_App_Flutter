import 'package:flutter/material.dart';
import 'package:task_manager/app.dart';

void main() {
  runApp(const TaskManager());
}
/// Usually This file is where before starting the app others things are initialized